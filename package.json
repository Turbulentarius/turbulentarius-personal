{
  "dependencies": {
    "bootstrap": "^5.3.3"
  },
  "scripts": {
    "build": "sass src/styles/bootstrap.scss build/css/main.css",
    "build-watch": "sass src/styles/bootstrap.scss build/css/main.css --watch",
    "dev": "concurrently \"npm run build\" \"live-server --entry-file=turbulentarius.html ./\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "live-server": "^1.2.2",
    "sass": "^1.85.1"
  }
}
